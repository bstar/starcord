[package]
name = "starcord"
version = "0.0.2"
edition = "2021"
rust-version = "1.90"
description = "A Winamp-feel terminal Discord client"
license = "MIT"
readme = "README.md"
repository = "https://github.com/bstar/starcord"
keywords = ["discord", "tui", "chat", "winamp", "terminal"]
categories = ["command-line-utilities"]

[dependencies]
# The shared terminal foundation: paths, logging, atomic and private file
# writes, theming, the dock layout engine, text entry, wrapping and the image
# pipeline, all of it already proven in STAR/AMP. Only what a chat client
# needs: `wsz` is a Winamp skin format nothing here reads, and `default-features
# = false` is what keeps it out.
#
# It is also the one copy of `ratatui`, `crossterm`, `ratatui-image` and
# `image` in the tree. Everything under `src/ui/` reaches them through
# `starkit::`, and none of the four is a direct dependency here: a widget built
# against a second copy of ratatui does not satisfy a signature expecting the
# first, and the compiler reports that as two versions carrying the same
# number.
#
# `src/paths.rs` stays local rather than becoming a wrapper over
# `starkit::paths::Paths`: the shared type has no `session_file()` or
# `media_cache_dir()`, and the core takes `crate::paths::Paths` by value
# throughout.
#
# A sibling checkout is used through an uncommitted `.cargo/config.toml`, as
# AGENTS.md describes.
#
# The `version` is not redundant with the tag: cargo-deny counts a dependency
# with no version requirement as a wildcard and `wildcards = "deny"` refuses
# it, whether or not the source is a git URL pinned to a tag.
starkit = { git = "https://github.com/bstar/starkit", tag = "v0.3.0", version = "0.3", default-features = false, features = [
    "image",
    "gif",
    "net",
] }

# CLI
clap = { version = "4", features = ["derive"] }

# Errors. `anyhow` where a failure is only ever reported, `thiserror` where the
# caller has to tell one failure from another -- HTTP, above all, where a 401
# stops the client and a 429 only delays it.
anyhow = "1"
thiserror = "2"

# Logging — file only. stdout belongs to the alternate screen, and a token or a
# message body must never reach either.
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
tracing-appender = "0.2"

# The async runtime, confined to one OS thread that the UI never sees. Only the
# parts the core uses: `net` for sockets, `fs` for the media cache, `time` for
# heartbeats and backoff, `sync` for the channels and the state lock.
tokio = { version = "1", features = [
    "rt-multi-thread",
    "sync",
    "time",
    "macros",
    "net",
    "fs",
] }
# CancellationToken. A gateway connection has three reasons to stop -- a
# reconnect, a logout and a shutdown -- and all three have to reach a `select!`
# that may be parked on a socket read.
tokio-util = "0.7"

# The gateway socket. Hand-rolled protocol on a plain websocket, because
# Discord's user-account gateway is not what any bot library models.
# webpki roots rather than the platform store: the only host it ever dials is
# Discord's, and a container with no ca-certificates should still connect.
tokio-tungstenite = { version = "0.30", default-features = false, features = [
    "connect",
    "handshake",
    "rustls-tls-webpki-roots",
] }

# REST. `rustls-no-provider` rather than `rustls`: the plain feature pulls
# aws-lc-rs, a C and assembly TLS backend, where this crate installs `ring` as
# the process default instead (see `http::install_crypto_provider`, and the
# test that fails if aws-lc reappears in the graph).
reqwest = { version = "0.13", default-features = false, features = [
    "rustls-no-provider",
    "json",
    "http2",
    "stream",
    # The fallback path for an attachment. Discord's two-step upload is the
    # normal one; when the attachments endpoint refuses, the file goes up in the
    # message itself as `payload_json` plus `files[0]`, which is multipart.
    "multipart",
    # A cookie jar, so the `__dcfduid`/`__sdcfduid` pair `discord.com/app`
    # sets comes back on the login that follows. Without them a login-class
    # request is answered with a captcha. Pure Rust (`cookie_store`).
    "cookies",
] }

# The provider itself, so `ring` is a direct and visible choice rather than
# whatever a transitive default happens to select.
rustls = { version = "0.23", default-features = false, features = [
    "ring",
    "std",
    "tls12",
] }

# The trust anchors, for the websocket and the REST client alike. Mozilla's CA
# list compiled in, rather than the host's store: this only ever dials Discord,
# a container with no `ca-certificates` package should still connect, and the
# two transports disagreeing about what they trust is the kind of difference
# that is only discovered in somebody else's deployment.
webpki-roots = "1"

# The gateway is `compress=zlib-stream`: one deflate context shared across every
# frame for the life of the connection, which no per-message decompressor can
# do.
flate2 = "1.1"

# Wire formats. `raw_value` matters: a dispatch envelope is decoded once to read
# `op`/`t`/`s`, and its payload is only parsed if the event is one this client
# handles, so an unknown event costs nothing.
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["raw_value"] }

# Config and the credentials file on disk.
toml = "0.9"

# Timestamps. `jiff` rather than `chrono` or `time` because message headers are
# rendered in the reader's local zone, and it is the one crate that carries a
# tzdb and gets local time right without a separate database crate.
jiff = { version = "0.2", features = ["serde"] }

# The token store. 4.2 is the rewrite over `keyring-core`; its `v1` default
# feature selects the Apple Keychain on macOS and a pure-Rust secret-service
# client over zbus on Linux, so no target gating and no libdbus.
keyring = "4.2"

# `X-Super-Properties` is base64 of a JSON object, and remote auth exchanges
# base64url keys and nonces.
base64 = "0.22"

# Parsing and joining API and CDN URLs, and -- the reason it is here rather than
# formatting strings -- deciding by host whether a request may carry the token.
url = "2"

# Lock-free publication of the connection status and the current token, both
# read on paths that must not block on a writer.
arc-swap = "1"

# The event side of the Handle. The UI loop is synchronous and drains this once
# per frame; a tokio channel would need a runtime on the UI thread.
crossbeam-channel = "0.5"

# IDENTIFY capabilities are a bitfield, and named flags are the difference
# between a reviewable constant and a magic number.
bitflags = "2"

# `apply` returns the events one dispatch produced, which is almost always none
# or one. Inline storage keeps that off the heap on the gateway's hot path.
smallvec = { version = "1", features = ["union"] }

# Stream and Sink for the websocket. tokio-tungstenite hands back a type that
# implements both and re-exports neither trait.
futures-util = { version = "0.3", default-features = false, features = ["sink", "std"] }

# Jitter on heartbeats and reconnect backoff, and the nonce on an outgoing
# message. Jitter is not cosmetic: without it every client that lost the same
# connection redials at the same instant.
rand = "0.9"

# The unicode emoji table, for the markdown parser. A table rather than a
# character-range test because the ranges are not the answer: whether a
# sequence of code points is one emoji is a lookup, and getting it wrong means
# measuring a family of four as four people and ruining the line.
emojis = "0.9"

# Decoding pictures. Attachments, avatars, custom emoji and GIFs all arrive as
# bytes somebody else produced, so the formats are named explicitly rather than
# taken as a default: png, jpeg, webp and gif are what Discord serves, and every
# other decoder in the crate is one more parser reading hostile input for no
# reason. This becomes STAR/KIT's re-export when that dependency lands, so that
# the core and the terminal image pipeline cannot end up on two versions.
image = { version = "0.25", default-features = false, features = [
    "png",
    "jpeg",
    "gif",
    "webp",
] }

# Naming a cached file by its URL. A hash rather than the URL itself because an
# attachment URL is a signed thing hundreds of characters long, and because a
# path built out of somebody else's string is a path that can escape the
# directory it was meant to be in.
blake3 = "1"

# The QR login. RSA-2048 with OAEP-SHA256 is what Discord's remote-auth gateway
# speaks: this client generates the key, Discord encrypts a nonce, a ticket and
# finally the token to it, and nothing but this process can read any of them.
rsa = "0.9"
sha2 = "0.10"
# `rsa` 0.9 takes its randomness through `rand_core` 0.6, which is a different
# crate version from the `rand` 0.9 used for jitter and nonces. `getrandom` is
# what makes `OsRng` exist there; a key generated from anything else is not a
# key.
rand_core = { version = "0.6", features = ["getrandom"] }

# Turning the login URL into the square the phone reads. Only the matrix is
# produced here -- drawing it is the terminal's business, and the core does not
# draw -- but generating it here is also what proves the URL encodes at all.
qrcode = { version = "0.14", default-features = false }

# Handing a plain link to whatever the desktop uses for links. Video and images
# go to the configured player instead, by argv and never through a shell.
webbrowser = "1"

# The system clipboard, for `y` and `Y`. `wayland-data-control` is what makes a
# copy survive the program exiting under a Wayland compositor: without it the
# clipboard is owned by a process that has gone away, and the paste lands
# nowhere. `image-data` is what `ctrl+v` on a picture needs: without it the
# clipboard is text only and a pasted screenshot would be silently nothing. It
# brings no decoder with it -- the bytes arrive as raw RGBA and are encoded to
# PNG with the `image` crate that is already here.
arboard = { version = "3.6", default-features = false, features = [
    "wayland-data-control",
    "image-data",
] }

# The quick switcher's ranking. The same matcher Helix and nucleo's own picker
# use, as a library rather than a picker: this supplies the list and the
# ordering, and the overlay draws it. A hand-written subsequence match would
# rank `general` below `gen-e-r-a-l` in some other channel's name, which is
# exactly the difference a fuzzy matcher exists to get right.
nucleo-matcher = "0.3"

# A reaction is an emoji in a URL path. The unicode case is four bytes that are
# not ASCII and the custom case is `name:id`, so neither can be formatted into a
# path as it stands; this is the encoder, with the set spelled out rather than
# left to whatever a URL type happens to do to a segment.
percent-encoding = "2"

# What to call the bytes of a file somebody attached. Discord's upload endpoint
# wants a content type and the only evidence available is the name, so the
# guess is made from the extension and falls back to `application/octet-stream`.
mime_guess = "2"

# Desktop notifications. Pure Rust: it reaches the notification daemon over
# zbus, so there is no libdbus and no `-sys` crate, which is the rule this tree
# keeps. Delivery happens on a blocking thread because the call is synchronous
# and talks to another process.
notify-rust = { version = "4.18", default-features = false, features = ["z"] }

[dev-dependencies]
# `start_paused` runtimes, so the rate limiter's sleeps are asserted rather
# than waited out.
tokio = { version = "1", features = ["test-util"] }
# Anything that parses foreign input gets a property test: the gateway
# inflater, the model deserialisers and the markdown parser all read bytes
# somebody else wrote.
proptest = "1"
# A real HTTP server for the rate limiter, which is a pile of header arithmetic
# that only a real response can exercise honestly.
wiremock = "0.6"
# Token-store and path tests write real files with real modes.
tempfile = "3"
# The UI's snapshots. A layout regression is a diff of a drawn frame, which is
# the only form in which anybody can actually see it; `insta` keeps the
# accepted frame beside the test and shows the change as a diff.
insta = "1"

[profile.release]
# Arithmetic on sequence numbers, sizes and offsets read off the wire. Wrapping
# silently in release turns a hostile number into a plausible wrong one, and the
# debug-built test suite would never see it.
overflow-checks = true
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true

# Decompressing and decoding what the gateway sends must not be slow in a debug
# build, or nothing can be tested without a release compile.
[profile.dev.package."*"]
opt-level = 2

[package.metadata.deb]
maintainer = "bstar <bob@dinicolas.com>"
copyright = "2026, bstar"
license-file = ["LICENSE", "0"]
extended-description = """\
A terminal Discord client built to feel like Winamp: dockable panels, sixteen
themes, and pictures drawn where the terminal can draw them. Real-time chat
with friends and servers, DMs and threads, emoji, reactions and GIFs. Voice,
video and server administration stay in Discord's own app."""
section = "net"
priority = "optional"
assets = [
    ["target/release/starcord", "usr/bin/", "755"],
    ["README.md", "usr/share/doc/starcord/README.md", "644"],
    ["packaging/starcord.desktop", "usr/share/applications/starcord.desktop", "644"],
    ["packaging/starcord.png", "usr/share/icons/hicolor/256x256/apps/starcord.png", "644"],
    ["packaging/starcord.svg", "usr/share/icons/hicolor/scalable/apps/starcord.svg", "644"],
]
# $auto resolves the real sonames from the built binary, which here is the C
# runtime and libgcc and nothing else -- so the package has to be built on a
# Debian-like host. Building it on NixOS produces a package with no dependency
# information at all.
depends = "$auto"
# Messages are full of emoji, and only the terminal's font can draw the unicode
# ones. A Recommends rather than a Depends: a chat client should not refuse to
# install over a missing glyph.
recommends = "fonts-noto-color-emoji"
